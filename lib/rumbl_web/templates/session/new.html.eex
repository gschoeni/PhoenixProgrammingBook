<h2>Login</h2>
<%= form_for @conn, session_path(@conn, :create), [as: :session], fn f -> %>

    <div>
        <%= text_input(f, :login, placeholder: "Email or Username") %>
    </div>

    <div>
        <%= password_input(f, :password, placeholder: "Password") %>
    </div>

    <%= submit("Log in") %>
<% end %>
